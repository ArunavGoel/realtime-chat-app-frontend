import React from 'react';
import ReactDOM from 'react-dom';

//Every thing that we will create in React will be inside this App, and that will be put inside the "root" div of index.html
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));